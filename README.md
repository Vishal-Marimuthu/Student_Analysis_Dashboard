# Project Structure

This project has been restructured into separate frontend and backend directories.

## Directory Layout

- **frontend/**: Contains all the client-side code (React/Vite application).
- **backend/**: Reserved for server-side code (currently empty).

## Getting Started

### Frontend

Navigate to the `frontend` directory to run the application:

```bash
cd frontend
npm install
npm run dev
```

### Backend

(Add backend instructions here when available)
